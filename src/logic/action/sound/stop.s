; stop.sound();

; If a sound is playing, it is stopped. The flag that was given as the second
; parameter of the sound command is set when this happens.

logic.action.stop.sound:

    jp logic.action.nyi
