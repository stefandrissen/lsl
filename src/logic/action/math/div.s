
; div.n(vA,B);

; vA is divided by B. The result is rounded down. If you divide by 0, the
; interpreter will crash.

logic.action.div.n:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; div.v(vA,vB);

; vA is divided by vB. The result is rounded down. If you divide by 0, the
; interpreter will crash.

logic.action.div.v:

    jp logic.action.nyi
