; reverse.loop(oA,fB);

; Object oA is cycled in reverse order until the first cel in the loop is
; reached. Flag fB is reset when the command is issued, and when the first cel
; is displayed fB is set.

logic.action.reverse.loop:

    jp logic.action.nyi
