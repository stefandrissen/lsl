; stop.update(oA);

; This tells the interpreter not to update object oA at the start of every
; cycle. The object remains on screen, unchanged.

logic.action.stop.update:

    jp logic.action.nyi
