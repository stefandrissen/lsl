; ignore.blocks(oA);

; Object oA is allowed to cross over conditional barriers (pixels set to color 1
; on the priority screen) and borders of blocks set up with the block command.

logic.action.ignore.blocks:

    jp logic.action.nyi
