; reverse.cycle(oA);

; This tells the interpeter to cycle object oA in reverse order, so the cels are
; shown from last to first. To turn cycling off, use the stop.cycling command.

logic.action.reverse.cycle:

    jp logic.action.nyi
