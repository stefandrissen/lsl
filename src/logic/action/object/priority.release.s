; release.priority(oA);

; If object oA's priority was previously set, it is "released" which means that
; the priority now depends on the position of the object.

logic.action.release.priority:

    jp logic.action.nyi
