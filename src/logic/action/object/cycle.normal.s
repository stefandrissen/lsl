; normal.cycle(oA);

; This tells the interpeter to cycle object oA in normal order, so the cels are
; shown from first to last. To turn cycling off, use the stop.cycling command.

logic.action.normal.cycle:

    jp logic.action.nyi
