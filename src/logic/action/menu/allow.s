
; allow.menu(A);

; This command will determine whether the menu is activated or not.
; If A = 0 then the menu is turned off.
; If A = 1 then the menu is turned on.

logic.action.allow.menu:

    jp logic.action.nyi
