
; hide.mouse();

; This command hides the mouse cursor. This command does not work with Sierra's
; PC version of the interpreter.

logic.action.hide.mouse:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; show.mouse()

; This command makes the mouse cursor appear on the screen. This command does
; not work with Sierra's PC interpreter.

logic.action.show.mouse:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; fence.mouse(A,B,C,D);

; This command sets up a fence in which the mouse cursor can not go out of. This
; command does not work with Sierra's PC version of the interpreter.

logic.action.fence.mouse:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; mouse.posn(x,y);

; This command sets the variables x and y to the x and y position of the mouse.
; This command does not work with Sierra's PC version of the interpreter.

logic.action.mouse.posn:

    jp logic.action.nyi

;-------------------------------------------------------------------------------
