
; echo.line();

; The previous line of input entered by the player is added to the input line
; (if there is already text on the input line, only the characters from the
; previous line after the number of characters currently on the line are added).

logic.action.echo.line:

    jp logic.action.nyi

