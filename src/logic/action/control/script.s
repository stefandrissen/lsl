
; push.script();

; This command is used if you want to load something then unload it so it does
; not use too much of the interpreter script. This can stop an out of script
; problem

logic.action.push.script:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; pop.script();

; This command is used if you want to load something then unload it so it does
; not use too much of the interpreter script. This can stop an out of script
; problem.

logic.action.pop.script:

    jp logic.action.nyi
