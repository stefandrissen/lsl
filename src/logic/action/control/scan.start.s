
; set.scan.start();

; Tells the intepreter to start execution of this logic from the current point,
; instead of from the start of the logic.

logic.action.set.scan.start:

    jp logic.action.nyi

;-------------------------------------------------------------------------------

; reset.scan.start();

; Tells the interpreter to start execution of the logic from the start of the
; logic.

logic.action.reset.scan.start:

    jp logic.action.nyi
